package lesson5.zoo;

import lesson5.Animal;

public class Cat extends Animal {
    public Cat(String type, String name, float run, float swim, float jump) {
        super(type, name, run, swim, jump);
    }
}
